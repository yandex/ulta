# Версия, которой агент представляется бэкенду: именно она заводится в agent_versions
# и участвует в TARGET. Поднимается руками и редко — при осмысленной смене версии агента.
#
# Версия ПАКЕТА и тега образа — другая: релизный флоу дописывает к этой константе номер
# релиза CI (1.1.18.66), чтобы каждый релиз был уникален и не требовал бамп-PR (LOAD-3524).
# Вшивать номер релиза в бинарь и репортить его нельзя: custom_version не участвует в
# идентификаторе сборки, и при тёплом кэше в бинарь попадает версия чужой сборки —
# проверено пробником (пакет 1.1.18.777 с бинарём, который печатает 1.1.18.999).
VERSION = '1.1.18'
